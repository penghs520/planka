server:
  port: 18106

spring:
  application:
    name: comment-service
  # Nacos 服务注册配置
  cloud:
    nacos:
      discovery:
        server-addr: localhost:28848
        namespace: public
  datasource:
    url: jdbc:mysql://localhost:23306/planka_comment?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  # Redis配置
  data:
    redis:
      host: localhost
      port: 26379
      database: 0
      timeout: 3000ms
  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: comment-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: cn.planka.event,cn.planka.event.comment

# MyBatis Plus
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: input

# 评论配置
comment:
  # 撤回时间限制（秒）
  withdraw-time-limit: 120

# Logging
logging:
  level:
    cn.planka: DEBUG
